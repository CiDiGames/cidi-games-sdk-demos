# Go Demo Server

Go server demo for CIDI Game SDK offline mode.

## Layout

- `src/`: server source files

## Expected Endpoints

Implement the shared API contract from `../../shared/protocol/api-contract.md`.
